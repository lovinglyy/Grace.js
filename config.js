const config = {};
config.botOwner = 'bot owner id';
config.token = 'bot token';
config.defaultPrefix = '--';
config.youtubeAPI = 'youtube v3 api key';

Object.freeze(config);

module.exports = config;
